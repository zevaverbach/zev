.. highlight:: shell

============
Installation
============


Stable release
--------------

To install zev, run this command in your terminal:

.. code-block:: console

    $ pip install zev

This is the preferred method to install zev, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for zev can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/zevav/zev

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/zevav/zev/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/zevav/zev
.. _tarball: https://github.com/zevav/zev/tarball/master
